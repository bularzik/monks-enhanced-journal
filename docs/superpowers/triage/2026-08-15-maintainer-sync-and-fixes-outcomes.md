# Maintainer 14.00 Sync + Discord Fix Round — Outcomes

Date: 2026-08-15
Spec: `docs/superpowers/specs/2026-08-15-maintainer-sync-and-fixes-design.md` · Plan: `docs/superpowers/plans/2026-08-15-maintainer-sync-and-fixes.md`
Base: `backlog-fixes` @ 964bf19 (frozen, PR #821 tip) · Sync: `maint/14.00-sync` @ b653a3a (pushed) · Integration: `integration-14.07` @ 0506df3 (pushed) · Release: [14.07-test](https://github.com/bularzik/monks-enhanced-journal/releases/tag/14.07-test) (annotated tag at 0506df3, pushed)

Install URL: `https://github.com/bularzik/monks-enhanced-journal/releases/download/14.07-test/module.json` — older test manifests NOT repointed. No PRs raised (each branch becomes its own upstream PR when upstream is ready, subject to the current PR #821 "no further additions" freeze — see Open decisions). Post-publish `release-smoke.mjs` against the live manifest: **SMOKE PASSED** (fresh manifest install, version `14.07-test` confirmed, all 13 MEJ `JournalEntryPage` types + `ShopSheet` registered, environment restored).

Reconciliation report: [`docs/superpowers/triage/2026-08-15-maintainer-14.00-reconciliation.md`](2026-08-15-maintainer-14.00-reconciliation.md) (on this branch, `bf1576e`).

## Branches (pushed)

`integration-14.07`, `maint/14.00-sync`, `playwright-harness`, `enhancements-test`, `fix/encounter-controlicon`, `fix/tableresult-deprecation`, `fix/encounter-placement-scene`, `fix/large-font-headers`, tag `14.07-test`. **Not pushed**: `backlog-fixes` (frozen, confirmed unchanged at `964bf19` on both local and origin throughout this round). No PRs raised.

## Per-phase results

| Phase | Branch (HEAD, pushed) | Result |
|---|---|---|
| A — Maintainer 14.00 reconciliation | `maint/14.00-sync` (b653a3a; sync content landed at 9cb5db6, later advanced by Phase C's player-open fixes and a Task-13 gate fix) | Synthetic three-way merge of the maintainer's private-build zip (`tmp/maint-zip` @ 4d392dc) against `backlog-fixes` @ 964bf19, rule **keep-ours-unless-additive**. 46 raw conflict hunks → 16 ruling entries; **12 flagged `FLAG-DELIBERATE-REVERT`** (maintainer attempted an independent fix to the same bug we'd already fixed — kept ours in every case, each written up standalone in the reconciliation report for Dan's review) + 4 plain `kept-ours` + 2 `took-theirs`/combined + 1 combined/deduped. Genuine maintainer additions layered in: compendium "Open Outside Enhanced Browser" context menu, defunct-relationship display support, locked-compendium editability template guards, sidebar-collapse persistence, v14 API modernization (`ForcedDeletion`, namespaced `ChatMessage`, context-menu icon strings), `apps/selectplayer.js` + `assets/defunct.png` (added but unreferenced — dead scaffolding either way). Full detail, including the 65-file "maintainer deleted but we didn't propagate" list (debugging artifacts/docs only, no lost functionality) and the re-raise list of our own #820/#821 commits absent from the maintainer's build: reconciliation report linked above. |
| B — Tests for maintainer changes | `playwright-harness` (b366639) | Behavioral maintainer-change clusters covered by `maintainer-features.mjs` (11 clusters: 9 behavioral, 2 cosmetic) on `playwright-harness` @ c97d42c, 2 fix rounds to the spec itself before APPROVED. **Bug found while writing this spec, not fixed this round**: maintainer's own `SlideshowSheet` autoplay is dead code on two counts — `_preFirstRender` is never called in the tabbed-sheet render flow, and `playSlideshow` throws on `trueElement` when it is reached by another path — plus an unrelated pre-existing `sheet.object` AppV1-alias error surfaced in the same investigation. All three logged below as follow-up candidates, not fixed (out of scope for a test-writing task). |
| C — Seven Discord-issue fix branches | see per-issue table below | 5 issues fixed (own branches, each off `backlog-fixes` @ 964bf19 so each can become its own #821-stacked PR later); 2 issues resolved to documented verdicts with no branch (`removeAttribute` = core-side/needs-info, `dialog-button-overlap` = needs-info/airtight — three trees checked, no repro). |
| D — Integration + release gate | `integration-14.07` (87fd5ec → 0506df3 after the Task-13 gate fix) | `enhancements-test` @ bf1576e + `maint/14.00-sync` + all 4 fix branches, merged `--no-ff` in that order. **1 conflict** (`monks-enhanced-journal.js`, `altOpensOutside`/`waitForFirstPage` region — additive-vs-nothing, resolved by keeping both sides' content, no functional loss). 8 invariants + maintainer-additions + dark-mode-survival all verified; syntax-checked (`node --input-type=module --check`) on all 13 changed `.js` files. Gate: full harness suite ×2 green (**17/17 both runs**), zero un-tolerated `monks-enhanced-journal` console errors. **Fix found during the gate, not before it**: `_toggleDisabled()` iterated the whole app-level `<form>` (`this.form.elements`, including directory-sidebar search/sort/collapse controls) instead of `this.trueElement` — a latent bug present since baseline (`964bf19`) that was silently inert because `get form()` used to return `undefined`; Task 8's `d844a4c` fix (within this same round, Phase A) made `get form()` resolve for real, which is what turned the latent bug into a live one (surfaced as `deep-search.mjs`/`maintainer-features.mjs` failures — the toggle-search-mode button intercepting/never receiving player clicks). Fixed on the owning branch, `maint/14.00-sync` @ `b653a3a`, re-merged clean into `integration-14.07` @ `0506df3`. One undiagnosed flake (not reproduced in isolation, didn't recur in either gate run): `discord-fixes.mjs` "Show to Players" socket-timing, 1 occurrence, documented not fixed. |

## Phase C — per-issue verdicts

| # | Discord thread | Verdict | Branch (HEAD) | Notes |
|---|---|---|---|---|
| 1 | Create Encounter Buttons | **Fixed** | `fix/encounter-controlicon` (57c14fc) | `ControlIcon#iconSrc` deprecated shim → `#texture`. Second finding beyond the console warning: the buggy code's next line (`controlIcon.texture = null`) immediately clobbered the just-set texture back to null, so the sword icon never actually rendered during placement — not just a deprecation warning, a real visual regression. Both confirmed pre/post-fix live. |
| 2 | List, Create Entry (dialog button overlap) | **Needs-info (airtight)** | none | No repro of Save-Changes icon/text overlap on `964bf19`, `maint/14.00-sync`, or `tmp/maint-zip` (verbatim reporter tree) at font scale 5/10 (default and max). No MEJ rule styles those buttons on any of the three trees checked. Screenshot + computed-style evidence in `task-11-report.md`. |
| 3 | MATT Start Encounter | **Fixed** | `fix/encounter-placement-scene` (7edd1db) | `this.scene` → `canvas.scene?.` in the `startencounter` tile action's `location` restrict callback. Root cause: the callback is an arrow function nested in another arrow function at ES-module top level, so `this` is structurally `undefined` on every invocation regardless of caller — not a null-scene edge case. Verified with a real, live Monks Active Tiles 14.01 repro (not code-evidence-only): exact reported crash reproduced pre-fix, resolved post-fix. |
| 4 | Players can't open the journal | **Fixed (folded into sync)** | ships inside `maint/14.00-sync` (commits `88bf462`/`ebe8538`/`cb0a98c`/`d844a4c`) | Root-caused to the merge-family `get form()` nested-form lookup bug (broke Show-to-Players) plus a companion `_onSubmitForm` sibling bug (silent edit loss) — both introduced by the maintainer/our-work reconciliation itself, not present as a standalone reporter-side defect once the sync's other fixes are in place. No separate `fix/player-open-journal` branch exists; the fix lives where the bug was introduced. One literal symptom from the thread ("sidebar click does nothing") was never independently reproduced — logged, not chased further. |
| 5 | Relationship Header & Large Font Size | **Fixed** | `fix/large-font-headers` (0cd3219) | Three `header`-scoped CSS rules (items-list/customise-dialogs, loot-characters, relationship list) used a fixed `height: 30px` that clipped/squashed content at large `core.uiConfig.fontScale`; replaced with `height: auto; min-height: 30px`. Screenshot-verified at both font scales. |
| 6 | removeAttribute Error | **Core-side / needs-info** | none | Foundry core's own `JournalEntrySheet._renderPageView` has a TOCTOU race: `render()` can resolve without ever assigning `sheet.element` if `_canRender` throws on a visibility flip between the container's page-visibility filter and the per-page sheet's own check, then `_renderPageView` calls `.removeAttribute` on that undefined element. MEJ's own equivalent code path is already immune (pre-existing guard at `sheets/JournalEntrySheet.js:614`, commit `08ffca5`); the crash is only reachable via the core-native sheet fallback with zero MEJ code in the stack. Root-cause receipts (core source line references) in `task-10-report.md`. Candidate for an upstream Foundry core bug report — Dan's call (see Open decisions). |
| 7 | rollTable Deprecation | **Fixed** | `fix/tableresult-deprecation` (65b8e27) | `TableResult#text` (removed v15) → type-branched read (`tableresult.type === "text" ? tableresult.description : tableresult.name`) at the single shared `rollTable()` implementation used by all four "populate from roll table" entry points (Quest, Loot, Shop, Encounter) — confirms the reporter's "probably Loot too" suspicion. |

## Suite results

Full harness suite (17 specs), release gate on `integration-14.07`: **2 consecutive green runs, 17/17 both times**, zero un-tolerated `monks-enhanced-journal` console errors (6 hits both runs, all inside two pre-documented tolerated blocks). Post-publish `release-smoke.mjs` against the live 14.07-test manifest: **PASSED**.

## Logged (not fixed) — follow-up candidates

- **MAT `controlEntity` path `entity.parent?.id`** — the encounter-placement `restrict` callback's guard likely should read `entity.document?.parent?.id` for placeables MAT's `controlEntity` path passes; pre-existing, never worked, out of scope for the Task 7 minimal fix (`fix/encounter-placement-scene`).
- **`Tile` global deprecation warning** — same line/area as the encounter-placement fix; a v13-sweep candidate, not addressed this round.
- **Quest `rollTable()` tail throw** — pre-existing: throws when `rewards[rewardId]` is undefined; UI-gated by `{{#if reward}}`, only reachable programmatically. Found during Task 6 (`fix/tableresult-deprecation`) investigation, not in scope for that fix.
- **TOC `.headings li` fixed-height anti-pattern** — same shape as the header-clipping bug fixed this round (`css/monks-enhanced-journal.css`, `.journal-sidebar .toc .headings li`, fixed `--line-height: 32px` custom property), but on an `<li>` not a `header` element, so it fell outside Task 9's "header-scoped" grep criteria. Found during that task's sweep; not fixed.
- **`SlideshowSheet` autoplay dead code** — two defects found while writing Phase B's `maintainer-features.mjs`: `_preFirstRender()` is never called in the tabbed-sheet render flow, so the maintainer's own auto-play-for-non-owners feature never actually fires; separately, `playSlideshow()` throws on `trueElement` when reached via another path. Not fixed (out of scope for a test-writing task).
- **`sheet.object` AppV1 alias error** — pre-existing error surfaced incidentally during the same Phase B investigation; not diagnosed further or fixed.
- **`discord-fixes.mjs` "Show to Players" socket flake** — one occurrence during the Task-13 gate run, not reproduced in isolation, did not recur across either of the two green gate runs. Root cause undiagnosed; worth a closer look if it recurs.
- **Maintainer items not re-adopted** — the reconciliation's `keep-ours-unless-additive` rule meant several maintainer-side items never made it into `maint/14.00-sync` even though they aren't strictly regressions of our work: the locked-compendium `EnhancedJournalSheet.js`-level guards (deeper than the template-level `{{#if (and owner editable)}}` guards that *were* adopted), and the maintainer's `item.hide`/`hidden` typo fix (their code reads correctly where ours has the historical typo — flagged in the Task 3 report as a re-raise candidate, not carried over this round since it wasn't purely additive against our tree).
- **`apps/selectplayer.js` orphaned + missing template** — added cleanly by the reconciliation merge (new `SelectPlayer` class), but not referenced anywhere in the codebase, including the maintainer's own zip. Harmless dead code; also has no companion template file, so it could not function even if wired up. Noted for visibility, not a defect to fix.
- **Core `_renderPageView` TOCTOU race** (issue #6 above) — upstream-report candidate; MEJ-side is already immune, so this is purely a courtesy report to Foundry core if Dan wants to file it.
- **`assets/defunct.png`** — added by the reconciliation merge alongside the defunct-relationship display support, but unreferenced by any template/CSS currently in the tree (the feature's actual dimming comes from a CSS class hook, not this image). Dead asset, harmless.

## Open decisions (Dan's)

- **Raising fix branches as PRs**: when upstream is ready, raise `fix/encounter-controlicon`, `fix/tableresult-deprecation`, `fix/encounter-placement-scene`, and `fix/large-font-headers` as PRs on top of #821 (each is based off `backlog-fixes` @ 964bf19 for exactly this purpose). Per current repo policy, PR #821 itself (`bularzik:backlog-fixes`) has an upstream "no further additions" request — these would be separate PRs, not additions to #821.
- **The re-raise list**: the reconciliation report's "Our #820/#821 work absent from 14.00" table (13 commits — `4cc119f`, `47c9ac4`, `7aaebc5`, `7cc2140`, `b9ba286`, `0fbb3c1`, `65dd38a`, `efca9e1`, `59db6ea`, `f2a445d`, `b150f54`, `4ef1b6c`/`630fb1a`, `45b0704`) represents fixes the maintainer's 14.00 build lacks or (in 12 cases) attempted an inferior alternative to (the `FLAG-DELIBERATE-REVERT` list). Whether/when to communicate these to the maintainer directly (outside the normal PR flow, since several are already inside frozen #821) is Dan's call.
- **Repointing older test manifests** at 14.07-test — deliberately **not done** this round, per standing instruction (unchanged from every prior round).
- **Reporting the core `JournalEntrySheet._renderPageView` TOCTOU race upstream to Foundry** — MEJ-side has no exposure, so this is a pure-goodwill report; Dan's call whether it's worth filing.
- **PR #821 shop-UI cherry-pick recommendation** — unchanged from the 14.04b outcomes doc: PR #821 still carries the shop-UI bug fixed by the 14.04b cherry-pick (302e778+3d00a29, present on `enhancements-test`/`integration-14.07` but never merged back into frozen `backlog-fixes`).
