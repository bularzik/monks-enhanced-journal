# Upstream 14.01 vs PR #821 — per-commit classification (2026-09-18)

Upstream released 14.01 on 2026-09-13 as one squashed commit (`9d66fb9`, tag `14.01`) on top of `ec4feee`; PR #821 (`origin/backlog-fixes` @ `964bf19`, 91 commits) was not used, and neither PR #820 nor #821 has a maintainer comment. 14.01 is byte-identical to the maintainer's "candidate 3" zip (`~/mej-v14-testing/monks-enhanced-journal-3.zip`) except `lang/pl.json`, `module.json`, two templates, and the packaging files (`CHANGELOG.md`, `LICENSE`, `README.md`), so the 2026-09-02 candidate-2 review still applies; the c2→14.01 source delta is 767 diff lines (Start Encounter constructor fixed, slideshow Loop fixed, Replay button, note-palette hook split, sidebar Edit context entry, clear-image refactor, dnd5e light theme, list-progress dblclick, header-controls rewrite, actor-drop opens actor sheet).

Method: five read-only agents, one per area, ran `git show <sha>` per PR commit and inspected the same function in the 14.01 tree (`git show 14.01:<path>`). Statuses: PRESENT-SAME / PRESENT-ALT (complete | partial | broken) / ABSENT / OBSOLETE / N/A. Static analysis only — nothing below was verified live on 14.01.

Merge cost reference: `git merge-tree 14.01 origin/backlog-fixes` → 13 conflicting files, 99 conflict hunks (34 in `apps/enhanced-journal.js`, 26 in `sheets/EnhancedJournalSheet.js`, 12 in `monks-enhanced-journal.js`).

## Tally (85 non-chore commits)

| Status | Count |
|---|---|
| PRESENT-SAME | 15 |
| PRESENT-ALT complete | 6 |
| PRESENT-ALT partial or broken | 20 |
| ABSENT | 41 |
| OBSOLETE | 3 |
| N/A (chores, self-inflicted, translation) | 6 |

## Group 1 — v14 compat (#820) and early fixes

| sha | title | issues | status | 14.01 evidence (file:line) | note |
|---|---|---|---|---|---|
| 0976542 | v14 compatibility: manifest bump and hard runtime fixes | #820 | N/A (mostly) / PRESENT-ALT for one sub-item | module.json:4,207-208 (14.01, min/verified 14); monks-enhanced-journal.js:805-809 (compendium click uses `target.closest("[data-entry-id]")`, plus extra `if (!li) return wrapped`); apps/enhanced-journal.js:617-631 (scroll-position element-injection loop gone); apps/enhanced-journal.js:278-288 + 682-690 (controls-dropdown population commented out, `_getHeaderControls` merges subsheet controls); entryPartial/_onCreateDocument absent | Compendium click, scroll sync, and removed partials are equivalent. Autosave differs: 14.01 dropped the dead `JournalTextPageSheet.onAutosave` OVERRIDE but does NOT wrap `_onAutosave` to re-render the parent MEJ sheet; instead monks-enhanced-journal.js:1024-1028 patches `ProseMirrorEditor._handleAutosave` to skip autosave when `document._sheet` is null (a different concern). Also 14.01 never toggles `window.controls` hidden (only in commented code). |
| 3032807 | v14 compatibility: replace deprecated global references | #820 | PRESENT-ALT (partial) | apps/enhanced-journal.js:424, monks-enhanced-journal.js:464,466,1199 (VIEW_MODES namespaced); apps/customise-pages.js:170 (`foundry.utils.getType`); monks-enhanced-journal.js:1190 (`foundry.appv1.api.DocumentSheet`); sheets/JournalEntrySheet.js:715 (namespaced showDialog); sheets/JournalEntrySheet.js:306 still `Journal.showDialog(...)` bare global | One leftover deprecated bare `Journal` global in the page context menu "Show" entry (warns in v14, breaks in v15). |
| 5cd43fa | v14 compatibility: sync JournalEntrySheet fork and migrate context menu entries | #820 | PRESENT-ALT (callCloseHooks BROKEN; context-menu migration PRESENT-SAME) | sheets/JournalEntrySheet.js:1050: `const pages = pageId ? [this._pages["test"]].filter(p => !!p) : Object.values(this._pages);` vs core v14 journal-entry-sheet.mjs:1417 `(pageId in this._pages ? [this._pages[pageId]] : [])`; context menus: 0 `condition:` keys remain, `onClick:`/`visible:` used across apps/enhanced-journal.js, sheets/*.js, monks-enhanced-journal.js | 14.01 avoids the deleted-page crash but with a debug leftover: it looks up a hard-coded page id `"test"`, so when a pageId is passed the per-page close hooks / `_closeView` never fire for the real page (closeView hooks silently skipped on page switch). Ours matches core exactly. |
| 90f15a4 | v14 compatibility: replace legacy -=key deletion syntax with data operators | #820 | PRESENT-SAME | sheets/QuestSheet.js:166,500,802; monks-enhanced-journal.js:1883 (`new foundry.data.operators.ForcedDeletion()`) | Identical. |
| 0e732f8 | Remove dead code exposed by the v14 migration | #820 | PRESENT-ALT (partial) | apps/selectplayer.js, plugins/dcconfig.plugin.js, templates/selectplayer.html absent; sheets/ListSheet.js:64 (`?? "basic"`); ListSheet/SlideshowSheet `_render` overrides gone; defaultOptions blocks still present but inside `/* */` (EncounterSheet.js:70 etc.); monks-enhanced-journal.js:2431 still `img._render(true)` on an ImagePopout (other two sites fixed: :1228, :2980) | One of three broken ImagePopout `_render(true)` calls remains at monks-enhanced-journal.js:2431 (the LIMITED-permission open path): ApplicationV2 has no `_render`, so it throws TypeError. ListSheet subtype fix present. |
| d4a3c95 | Restore the Maximize window feature as a v14 header control | — | PRESENT-ALT (complete) | apps/enhanced-journal.js:82 (`fullscreen` action), :1535-1546 (`_getFrameButtons` unshifts Maximize button), :1557-1572 (`static fullscreen()`); core v14 application.mjs:738/882 confirms `_getFrameButtons` is a real hook | 14.01 exposes Maximize as a frame button rather than a controls-dropdown entry; works. Cosmetic: its icon swap toggles fa- classes on the `<button>` rather than its `<i>`, so the glyph may not change between Maximize/Restore; tooltip does. |
| 6e1e21c | 14.01 changes | — | PRESENT-SAME (code) / N/A (changelog) | sheets/EnhancedJournalSheet.js:479 `this.addItem(data)` | The `addItems`→`addItem` typo fix is present. CHANGELOG text differs (theirs lists different items). |
| 1a0eefa | Fix read-only sheets for players under v14 | — | PRESENT-ALT (complete) | sheets/EnhancedJournalSheet.js:952-976 (`_toggleDisabled` override using `this.trueElement` and `this.form \|\| this.enhancedjournal?.form`, skips `.tab.notes .editor-edit` / personal-notes textarea when a GM is active); apps/enhanced-journal.js:606-607 (calls `_toggleDisabled` without frame juggling) | 14.01's version is a superset (also respects locked packs, skips `.header-control`, disables `.item-controls a`). Minor: no null guard on `trueElement` (ours has `if (!element) return`). |
| 50c77c7 | Remove stray playwright console log | — | N/A | (deleted `.playwright-mcp/*.log` artifact only) | Repo hygiene. Unrelated: 14.01 ships a stray `console.log(size)` at sheets/EnhancedJournalSheet.js:3313. |
| d1969fc | Fix #803, #787, #767: guard quest save when no rewards exist | #803 #787 #767 | PRESENT-ALT (complete) | sheets/QuestSheet.js:441-448: `if (Object.keys(rewards).length != 0) { ...; let reward = rewards[this.getCurrentRewardId()]; if (reward) { reward.currency = ... } }` | Guards both empty rewards and missing current reward; arguably cleaner than our `\|\| {}`. |
| 10a97ac | Fix #780: quest objective drag ReferenceError | #780 | PRESENT-SAME | sheets/QuestSheet.js:572-575 (`let id = li.dataset.id;` hoisted above the `if (li.dataset.document == 'Item')` block) | Identical. |
| cd57a6a | Fix #95, #85: honor metaKey for open-in-new-tab on macOS | #95 #85 | ABSENT | monks-enhanced-journal.js:718, :736, :758 (and :689) all still `newtab: event.ctrlKey && !setting("open-new-tab")` with no `metaKey` | Cmd-click on macOS still does not open in a new tab. |
| cbfa053 | Fix: prevent Enter in journal title input from submitting | — | ABSENT | templates/sheets/partials/journal_pages.hbs:4-5 (no `onkeydown` handler); no Enter/keyCode 13 handling in sheets/JournalEntrySheet.js, sheets/EnhancedJournalSheet.js, apps/enhanced-journal.js | Enter in the title input still submits the form. |
| 488cba9 | Fix: exclude dnd4e from ApplicationV2 drop-item path | — | PRESENT-ALT (complete) | monks-enhanced-journal.js:206-210 `canDropOnActorSheet` excludes `["cyphersystem", "dnd4e", "dcc"]`; used at :4543 | Superset (also excludes dcc). Note :3690 and :4571 still call `sheet._onDropItem` without the system check. |
| cc1b5b1 | Fix #817, #677: honor Alt-click on map notes | #817 #677 | ABSENT | monks-enhanced-journal.js:1208 `if (! await MonksEnhancedJournal.openJournalEntry(this.entry, options))` — no `altKey` check anywhere in clickNote2 (:1194-1233) | Alt-click on a map note still opens in the Enhanced Journal instead of the native sheet. |
| f49225c | Fix #730: only set type thumbnail flag for MEJ types | #730 | ABSENT | monks-enhanced-journal.js:960 `await document.setFlag("monks-enhanced-journal", "img", \`modules/monks-enhanced-journal/assets/${type}.png\`);` unconditional (the `if (types[type])` guards at :951 and :962 surround it but do not cover it) | Non-MEJ page types (e.g. text/image/pdf/video created via the dialog) still get a bogus `assets/<type>.png` thumbnail flag. |
| f6b9689 | Fix #817, #677: pass real event to clickNote2 in no-libWrapper fallback | #817 #677 | ABSENT | monks-enhanced-journal.js:1239-1242: fallback still `return clickNote2.call(this, oldClickNote.bind(this));` (event discarded, bound core fn passed as `wrapped`) | Without lib-wrapper the event never reaches clickNote2 (and since cc1b5b1 is also absent, nothing reads it anyway). |

## Group 2 — shops, loot, currency, purchases

| sha | title | issues | status | 14.01 evidence (file:line) | note |
|---|---|---|---|---|---|
| 78a91b7 | hide hidden shop items from players | #805 | PRESENT-SAME | sheets/EnhancedJournalSheet.js:1812 `item.hidden !== true` | Identical field-name fix (`hide` -> `hidden`). |
| cd168fd | wire select-character prompt to ApplicationV2 action | #781 | PRESENT-SAME | templates/sheets/partials/sheet-shop-items.hbs:2 `data-action="openPlayerConfig"`; sheets/ShopSheet.js:15 action registered | Identical. |
| ded3b7f | convertItems read document from sheet, not options | #768 | ABSENT | sheets/ShopSheet.js:876 `this.options.document.getFlag(...)`, :885 `this.options.document.update(...)` | Original buggy `this.options.document` still there in convertItems; adjust-price still crashes on AppV2. |
| 492e701 | convert fractional currency through denominations | #706 #696 #477 | ABSENT | sheets/ShopSheet.js:371 & :414 `price.value = Math.floor(price.value * buy)`, :621 `data.sell = Math.floor(price.value * buy)`, :881 `Math.max(Math.ceil((price.value * sell), 1))`; helpers.js has no `distributeCurrency` | 14.01 still truncates fractional buy/sell prices; no denomination walk exists. |
| e97ac21 | stack repeat shop purchases | #796 | ABSENT | sheets/ShopSheet.js:596-600 unconditional `sheet._onDropItem(...)` / `createEmbeddedDocuments` | No `actor.items.find` merge; repeat purchases still create duplicate items. GM socket path (monks-enhanced-journal.js:3689-3694) likewise. |
| cb8e104 | add DCC system currency and drop-item support | - | PRESENT-ALT (partial) | monks-enhanced-journal.js:4288 `case "dcc":` currency table (same as ours); :206-209 `canDropOnActorSheet()` excludes dcc, used at mej.js:4543, EnhancedJournalSheet.js:3643, LootSheet.js:330/514/565; NOT used at mej.js:3690 (purchaseItem socket handler) or ShopSheet.js:596 (free purchase) which still call `sheet._onDropItem` ungated | Currency: same. Drop gate: 14.01 factored into a helper but missed two of the four sites we gated (GM-side purchase handler and player free-purchase), so DCC purchases via those paths still go through `_onDropItem`. |
| 1d05776 | use full spell-scroll UUID from dnd5e config | #791 | PRESENT-SAME | sheets/EnhancedJournalSheet.js:3242-3243 `spellScrollId?.startsWith("Compendium.") ? spellScrollId : ...` | Identical. |
| 2a83642 | call createScrollFromSpell on the correct class in addItem | #791 | PRESENT-SAME | sheets/EnhancedJournalSheet.js:2677 `EnhancedJournalSheet.createScrollFromSpell(itemData)` | Identical (no `EncounterSheet.` reference remains). |
| 6a6f87c | currency editor layout at large font sizes | #783 | ABSENT | css/monks-enhanced-journal.css:1699 `grid-template-columns: 80px auto 80px 40px;` | Fixed-px columns still present. |
| 3c0be9a | match DSA5 currency by the correct field | #795 | PRESENT-ALT (partial) | helpers.js:19-23 exports `currencyId()` but helpers.js:47 `defaultCurrency()` still `currency?.id`, helpers.js:117 `currency = tcurr.id`, sheets/ShopSheet.js:692 `this.getCurrency(actor, curr.id)` unchanged | 14.01 introduced the equivalent helper but only applied it inside `addCurrency` (EJS:1096-1156) and `onAcceptOffer` (EJS:3615-3633, misapplied there - see 5525855). The three sites this commit fixed (defaultCurrency, getPrice fractional branch, canAfford total) still resolve by placeholder id on dsa5. |
| 009f78e | parse all denominations of mixed WFRP4e prices | #521 | ABSENT | helpers.js:63-68 `for (let curr of ["pp","gp",...]) { if (...) { cost = ...; break; } }` | Still stops at first non-zero denomination; mixed gc/ss/bp prices lose the rest. |
| b2a25df | update remaining currency lookups for DSA5 name matching | #795 | ABSENT | helpers.js:187 `toDefaultCurrency`: `c.id == value.currency`; sheets/ShopSheet.js:695 `find(c => c.id == price.currency)` | Both lookups unchanged; `currencyId()` not used here. |
| 1d78cca | gate mixed-denomination parsing to wfrp4e's convert convention | #521 | ABSENT | helpers.js:63-68 (same loop as 009f78e) | Depends on 009f78e which is absent. |
| 2d7edb5 | use system-aware currency identifiers in distributeCurrency | #795 | ABSENT | helpers.js:126 `currency = curr.id;` (getPrice lowest-denomination fallback); no `distributeCurrency` in helpers.js | distributeCurrency hunks are moot (492e701 absent); the getPrice fallback hunk is still buggy on dsa5. |
| 57c3e67 | never price a nonzero item at zero after conversion | #706 | ABSENT | sheets/ShopSheet.js:881 `Math.max(Math.ceil((price.value * sell), 1))` | Guard only exists to protect our `distributeCurrency` result; in 14.01 convertItems uses `Math.ceil` (never 0 for nonzero) but also never converts denominations. Port together with 492e701. |
| 5525855 | shops could pay 0 for low-value items, and offers deducted currency the actor no longer had | - | PRESENT-ALT (broken) / ABSENT | Offer guard: sheets/EnhancedJournalSheet.js:3614-3621 `let currId = currencyId(k); if (this.getCurrency(actor, currId) < v)`; deduction :3630-3633 `this.addCurrency(actor, currencyId(k), -v)`. Buy-price floor: ShopSheet.js:371/:414/:621 still `Math.floor(price.value * buy)` | 14.01 added an offering guard, but `k` is a string id (apps/make-offering.js:193 keys by field name = currency id) and `currencyId()` (helpers.js:19) reads `.name`/`.id` off it -> `undefined` on EVERY system. Guard becomes `NaN < v` (never fires) and `addCurrency(actor, undefined, ...)` either returns early (purchase-conversion on: denomIdx == -1) or writes `system.currency.undefined`. Accepting an offering with currency is broken in 14.01. Buy-price-zero guard: absent. |
| f2a445d | DSA5 purchase deduction resolved coins by placeholder id and silently failed | #795 follow-up | PRESENT-SAME | sheets/EnhancedJournalSheet.js:1096, 1099, 1100, 1132, 1141, 1156 all use `currencyId(...)` | Functionally identical to our `ident(c)` closure inside addCurrency. |
| bc4ede9 | addCurrency reused one update object across coin updates | - | ABSENT | sheets/EnhancedJournalSheet.js:1175 (pf2e), :1186 (mythras), :1196 (dsa5), :1213 (wfrp4e) `updates[\`system.quantity...\`] = v; promises.push(coinage.update(updates));` | Shared `updates` object still mutated per coin; multi-coin changes still corrupt. |
| 7f78eec | consumable purchases logged as quantity 0; qty-0 results no longer create journal items | - | ABSENT | sheets/ShopSheet.js:748-751 sets `result.quantity = 0` for consumables BEFORE `addLog`; monks-enhanced-journal.js:4567 `if (!!result)` in dropJournalSheetData | Both halves absent (mej.js:2647 and :4536 do use `(result?.quantity ?? 0) > 0`, but :4567 does not). |
| 0e3cf54 | dsa5 offering guard/deduction resolves currency by id, not name | #260 follow-up | PRESENT-ALT (broken) | sheets/EnhancedJournalSheet.js:3615 `let currId = currencyId(k);`, :3631/:3633 `currencyId(k)` | Intent matches ours (map id -> name on dsa5) but applied to a string key, so it yields `undefined` on all systems (see 5525855). Our version (`currencies.find(c => c.id == k)?.name ?? k`) is the correct form. |

## Group 3 — relationships, links, pages, drops

| sha | title | issues | status | 14.01 evidence (file:line) | note |
|---|---|---|---|---|---|
| 14db53e | Fix: sync linked relationship hidden flag | – | ABSENT | sheets/EnhancedJournalSheet.js:2645 `otherRelationship.hidden = !otherRelationship.hidden;` (onToggleRelationship) | 14.01 still blindly inverts the reciprocal instead of copying the toggled value, so the two sides can desync. The second hunk (addRelationship `hidden: this.document.parent.hidden`) was itself reverted by our 59db6ea; 14.01 uses `hidden: relationship.hidden` there (3212/3214). Our final branch state removed the cascade entirely (396f1a9), which 14.01 also lacks. |
| 9e5dd23 | Fix #260: surface locked-compendium relationship write failure | #260 | PRESENT-ALT (partial) | sheets/EnhancedJournalSheet.js:3177-3180 `if (this.document.pack) { ... if (pack.locked) return; }`; 3202 `this.document.setFlag(...)` un-awaited, no try/catch | 14.01 pre-checks the locked pack and returns silently — avoids the unhandled rejection but gives the user no warning, and any other write failure (permissions, reciprocal side) is still swallowed. |
| 65dd38a | Fix #800: don't silently drop text roll-table results | #800 | PRESENT-ALT (partial) | sheets/EnhancedJournalSheet.js:2373 `let unknownItems = []`, 2402-2403 `if (!tableresult.name) continue;`, 2404 `if (...type == 'loot')`, 2446-2449 `else { unknownItems.push(text); }`, 2545-2553 whispers `templates/unknown-items.html` chat card | Never creates an item; only reports. Reporting lives inside the `type == 'loot'` branch, so on shop/quest/encounter sheets plain-text results are still silently dropped. |
| 0fbb3c1 | Fix: show defunct relationships to GMs for cleanup | – | PRESENT-ALT (mostly complete) | sheets/EnhancedJournalSheet.js:735-748 builds a `defunct` group for GMs; templates/sheets/partials/sheet-relationships.hbs:16 adds class `defunct`; css/monks-journal-sheet.css:756-763 disables everything but the delete link; assets/defunct.png exists | Works for the normal case. Gaps: 14.01 does not backfill `relationship.id` from the flag key (legacy entries render `data-id=""` so delete is a no-op) and does not substitute a name (name is never persisted, so the row label is blank). |
| c6c81d4 | Fix #260: log the underlying error when a relationship write fails | #260 | ABSENT | sheets/EnhancedJournalSheet.js:3202 `this.document.setFlag("monks-enhanced-journal", "relationships", relationships);` — no await, no try/catch, no logging | Depends on the try/catch from 9e5dd23 which 14.01 does not have. |
| 07d648b | Fix #764: give rendered headings anchor ids | #764 | PRESENT-ALT (partial) | sheets/JournalEntrySheet.js:579-580 `element.dataset.anchor = slug; element.id = slug;` (same as ours); monks-enhanced-journal.js:733 `const target = event.currentTarget;` (JournalEntry handler, unchanged); :753 `const target = event.target;` (patched JournalEntryPage handler) | Heading ids are present. But Foundry 14 dispatches `_onClickDocumentLink` from a body-level listener (client/applications/ux/text-editor.mjs:783-796), so `event.currentTarget` is `<body>` and `target.dataset.hash` is always undefined for JournalEntry links; the page handler's bare `event.target` loses the hash when the click lands on the link's inner `<i>` icon. Ours used `event.target.closest("a[data-link]")` in both. |
| e5088e9 | Fix #810: drag text selection to create section link | #810 | ABSENT | sheets/JournalEntrySheet.js:334-347 only registers a TOC DragDrop (`dragSelector: ".toc :is([data-page-id], [data-anchor])"`); no `dragstart` listener on `.journal-entry-page`, no `getSelection` / `@UUID[...#...]` builder anywhere in sheets/, apps/ or monks-enhanced-journal.js | Feature/fix not present in any form. |
| a4ac77d | Fix #810: don't hijack drags inside the editor | #810 | ABSENT | (same as e5088e9 — no `_onDragSelection` in sheets/JournalEntrySheet.js) | Guard is moot without the drag-selection handler; 14.01 has neither. |
| 7aaebc5 | Fix: correct undefined page reference in JournalEntryPage click handler | – | PRESENT-SAME | monks-enhanced-journal.js:764-766 `MonksEnhancedJournal.fixType(this); return this.sheet.render(true, { anchor: target.dataset.hash });` inside the `patchFunc("JournalEntryPage.prototype._onClickDocumentLink", ...)` | The stray `page` reference is gone; 14.01 rewrote the handler as a libWrapper-style patch but uses `this` correctly. |
| 4df9b66 | Fix: register page sheet classes under both prefixed and unprefixed type keys | – | PRESENT-SAME | monks-enhanced-journal.js:2225 `types: [k, \`monks-enhanced-journal.${k}\`],` | Identical registration. |
| 59db6ea | Fix: keep reciprocal relationships hidden by default | – | PRESENT-ALT (design differs) | sheets/EnhancedJournalSheet.js:3212 `hidden: relationship.hidden` (owner path), :3214 `hidden: relationship.hidden` (socket emit), monks-enhanced-journal.js:2906 socket handler passes `hidden: data.hidden` | 14.01 never had our `parent.hidden` regression, but instead of base's/our `hidden: true` it mirrors the dropped relationship's flag. `_onDropRelationship` (496-502) passes raw drag data with no `hidden`, so the reciprocal back-link is visible to players by default — the opposite of base/our behaviour. Not broken, but a behaviour change. |
| efca9e1 | Fix: relationship delete cascade unset the wrong flag key, orphaning reciprocals | – | PRESENT-SAME | sheets/EnhancedJournalSheet.js:2600-2612 `deleteRelationship(id)`: `let otherId = this.document.parent?.id ?? this.document.id; ... page.unsetFlag('monks-enhanced-journal', \`relationships.${otherId}\`)` / `emit("deleteRelationship", { uuid: journal.uuid, id: otherId })` | Same key fix, split into a dedicated `deleteRelationship` routed from `onDeleteItem` (2579-2588). |
| 396f1a9 | Fix #808: relationship visibility toggles no longer cascade; Fix #771: actor links open the actor sheet; remove inert history retry loop | #808, #771 | ABSENT (#808), PRESENT-ALT partial (#771), ABSENT (history) | #808: sheets/EnhancedJournalSheet.js:2628-2648 `onToggleRelationship` still walks to the other page and flips `otherRelationship.hidden` (2645). #771: apps/enhanced-journal.js:1487-1491 drop of an Actor now does `actor.sheet.render(true, { focus: true })` (base called `this.open(actor)`), but monks-enhanced-journal.js:717-721 `Actor.prototype._onClickDocumentLink` still routes through `openJournalEntry`, whose Actor bail-out at :2416 is still `doc instanceof Actor && doc.type == "hazard"`, so non-hazard actor content links still land in MEJ (BlankSheet + "CannotOpenActorInEnhancedJournal" warning, apps/enhanced-journal.js:500-503). History: apps/enhanced-journal.js:1169-1184 still has the `do { ... result = this.changeHistory(idx) } while (!result ...)` loop on an async call. | Toggle cascade (#808) untouched. #771 fixed only for the drag-drop path, not for clicking @UUID actor links. Inert retry loop retained (harmless but dead logic). |
| 13628ab | Fix: person/typed pages no longer revert to text sheets after any update | – | ABSENT | monks-enhanced-journal.js:834-843 `let oldtype; ... //oldtype = this.type; ... delete data.type; ... if (oldtype) this.type = oldtype;` (oldtype never assigned → restore is dead); monks-enhanced-journal.js:4420-4423 `updateJournalEntryPage` hook has no `fixType(document)` call | Same commented-out code as base; typed pages still lose their in-memory type after an update. |
| bdc5584 | Fix: raw i18n keys shown as name placeholders on Picture and Text Entry pages | – | ABSENT | sheets/PictureSheet.js:41-45 `context.placeholder = "MonksEnhancedJournal.Picture"; return mergeObject(context, { placeholder: i18n("MonksEnhancedJournal.PictureName") })`; sheets/TextEntrySheet.js:47-51 same with `i18n("MonksEnhancedJournal.JournalName")`; lang/en.json:163 `"MonksEnhancedJournal.PictureName": "PictureName"`, no `MonksEnhancedJournal.JournalName` key at all | Picture page shows the literal "PictureName"; Text Entry page shows the raw key "MonksEnhancedJournal.JournalName". |
| 9607620 | Fix: revert drop-item guard on journal-to-journal drop path | – | PRESENT-SAME | monks-enhanced-journal.js:4571 `sheet._onDropItem({ preventDefault: () => { }, target: { closest: () => { } } }, data);` in `dropJournalSheetData` — unguarded, no `journal.createEmbeddedDocuments` fallback | Matches our final (reverted) state for this site. |
| b9ba286 | Fix: apply system drop-item guards consistently across all call sites | – | PRESENT-ALT (partial) | monks-enhanced-journal.js:206-210 `static canDropOnActorSheet(sheet)` (excludes cyphersystem/dnd4e/dcc, no pf2e — same set as ours); used at monks-enhanced-journal.js:4543, sheets/EnhancedJournalSheet.js:3643, sheets/LootSheet.js:330/514/565. NOT used at monks-enhanced-journal.js:3689 (`acceptItem`: `if (sheet._onDropItem)`) or sheets/ShopSheet.js:596 (`if (sheet._onDropItem)`) | Shared predicate exists (on MonksEnhancedJournal rather than MEJHelpers) and covers 5 of the 7 actor-sheet sites. The two remaining sites are bare `sheet._onDropItem` checks — and both previously had `&& game.system.id != "dcc"` in base, so 14.01 actually regressed the dcc guard there. |

## Group 4 — tabs, editor, theming, CSS, settings, search, header controls

| sha | title | issues | status | 14.01 evidence (file:line) | note |
|---|---|---|---|---|---|
| 1691861 | Fix #761: legible text on Black Marble background | #761 | ABSENT | css/apsjournal.css:328-330 — `[background-image='marbleBlack']` block still sets only `--mej-background`; no light-text variable overrides (solidBlack at :361-388 has them, marbleBlack does not); no `.editor-edit` rule | Original bug intact. |
| 1d5ce0d | Fix #748: resolve background assets relative to stylesheet | #748 | ABSENT | css/apsjournal.css:321-421 all `url('/modules/monks-enhanced-journal/assets/...')`; css/editor.css:3-19 fonts absolute; css/monks-enhanced-journal.css:178 loading.gif absolute | Every asset URL still root-absolute; breaks under route prefix. |
| 6c71725 | Fix: remove duplicate tabs for the same document | — | PRESENT-ALT | apps/enhanced-journal.js:137 `this.removeDuplicateTabs()` in `_preFirstRender`; body at :1101-1125 | Theirs dedupes the restored list against itself (which is what our follow-up 6f70028 did, not this commit's `this.document` variant). See 6f70028 for the defect in theirs. |
| f667465 | Fix #742: serialize tab activation and content assignment | #742 | PRESENT-ALT (partial) | apps/enhanced-journal.js:950-1023 `activateTab` uses `_activatingTab` timestamp: returns `false` if another activation started <1s ago (:952-953), sets :960, clears :1020; sheets/JournalEntrySheet.js:637 `if (!this.trueElement) return;` and :645 `toc?.querySelector` both present | Theirs drops overlapping activations instead of queueing them: a second click/open while one is in flight is silently ignored (addTab/open/removeTab callers get `false` and the new tab is left un-activated). The two JournalEntrySheet `_synchronizeSidebar` guards are present and identical. |
| b150f54 | Fix #742: serialize tab content assignment and unify queued entry points | #742 | ABSENT | apps/enhanced-journal.js:398 `this.renderSubSheet(options);` still un-awaited in `_onRender`; :908 `this.render(true)` in deleteEntity un-queued; :1025-1075 `updateTab` renders outside any guard; :1077-1098 `removeTab` | No queue at all. Because renderSubSheet is not awaited, `_activatingTab` is cleared (:1020) before subsheet content/`this.document` are committed, so the 1s guard does not even cover the content-assignment race this commit targeted. |
| 6f70028 | Fix: de-duplicate restored tabs on first render | — | PRESENT-ALT (broken) | apps/enhanced-journal.js:137 calls `removeDuplicateTabs()` BEFORE `this.tabs.active` is defined at :139; inside, :1112 `let activeEntityId = this.tabs.active(true)?.entityId;` | Same idea (keep first per entityId, carry active flag), but if the dropped duplicate was the active tab, `this.tabs.active` is not yet a function (plain array from `duplicate()`), so `_preFirstRender` throws TypeError and the journal fails to open. Also collapses all blank tabs (entityId undefined) into one. |
| be7c087 | Fix #785: register custom ProseMirror marks with the serializer | #785 | PRESENT-SAME | classes/prosemirror.js:12-26 uses `menu.schema.marks.size` / `attrs: { size }`; no `new ProseMirror.Schema`/`marks.fontsize`/`marks.color` monkey-patch anywhere in file | Functionally identical to ours. 14.01 also removed the "Change Text Colour" menu item entirely (only background-colour at :62-70 and apsj-template at :72-79). |
| 7cc2140 | Fix: add "Allow GM" setting to open journal entries outside MEJ (from PR #797) | PR #797 | ABSENT | settings.js:135 registers only `allow-player`, no `allow-gm`; monks-enhanced-journal.js:202-204 `canShowEnhancedJournal` = `game.user.isGM \|\| setting("allow-player")` (key is correct, `allow-player`); used at :1590,:1604,:1623,:2358,:5157 and apps/enhanced-journal.js:417,:1042 | 14.01 centralised the check into a getter (good) but GMs cannot opt out of MEJ. No `allow-gm` lang keys either. |
| 08ffca5 | Fix: guard tab/bookmark reorder drops, forward page options into new tabs, guard updateRecent and page render | — | PRESENT-ALT (partial) | PRESENT: apps/enhanced-journal.js:1435-1436 and :1466-1467 `if (!target) return;`; :1319 `entity?.id`. ABSENT: :1298 and :1310 `await this.addTab(entity)` — pageId/anchor options not forwarded; sheets/JournalEntrySheet.js:618-622 `_renderPageView` has no `if (!sheet.element) return;` | Drop guards + updateRecent guard present; page-option forwarding into new tabs and the page-render null guard are missing. |
| 47c9ac4 | Fix: slide text editor crash, text-colour ReferenceError, inert Change Background in tabbed view, and Quests HUD close button | — | PRESENT-ALT (partial) | SlideText: apps/slidetext.js:5-8 ctor no longer duplicates `object` (tempdata removed; reads `this.options.document` :77) — fixed. Text-colour: `_changeTextColourPrompt` removed from classes/prosemirror.js (no `TextSelection` ref) — obsolete. Change Background: prosemirror.js:155 `closest("div[entity-uuid]")`; tabbed view stamps `entity-uuid` on `.content` div at apps/enhanced-journal.js:576-579; sheets/EnhancedJournalSheet.js:509-514 still only stamps under `!this.enhancedjournal` — UNSURE whether inert in 14.01. Quests HUD: apps/objective-display.js:157-162 `close()` still does nothing unless `options.properClose` — window X button remains inert (only added `objdisp = null` :160) | 2 of 4 fixed/obsolete; Quests HUD close still broken; Change Background unverified. |
| ac68671 | Fix #169: scope pf2e journal styling into MEJ page sheets (also #773, #739) | #169 #773 #739 | ABSENT | sheets/EnhancedJournalSheet.js:506-546 `_onRender` has no `journal-entry-page`/`journal-page-content` class additions; only pf2e refs are :264-265 (enrichHTML) and :609-619 (commented out) | Standalone MEJ sheets get no pf2e page classes. |
| a7be3bc | Fix: pf2e trait/tag styling never applied in the tabbed MEJ view | — | ABSENT | apps/enhanced-journal.js:576-580 `.content` gets entity attrs + `themed theme-x` but no `journal-entry-page`; :591-592 still adds only `journal-page-content` to the subsheet classes | Descendant selector `.journal-entry-page .journal-page-content` still cannot match in tabbed view. |
| f7b3f3d | Fix: sheet theme picker saved a preference nothing read and styled a selector matching nothing | — | OBSOLETE | sheets/EnhancedJournalSheet.js:324-336 MEJ `onConfigureSheet`→`ApplicationSheetConfig` commented out; apps/enhanced-journal.js:264-272 uses core `DocumentSheetConfig`; :572-579 reads core `sheetThemes` and applies `themed theme-<x>` to `.content`; apps/sheet-configure.js:102 still has the dead `$(.sheet.${type})` selector but nothing instantiates it | Replaced by core theme config. Note the outer window still uses `colorScheme` only (:121-122) so only the content area follows the per-document theme. |
| 630fb1a | Fix: Configure Sheet menu entry dropped by the header-controls override | — | PRESENT-ALT (partial) | apps/enhanced-journal.js:278-288 `_getHeaderControls` = `super._getHeaderControls()` (includes window.controls configureSheet at :67-72) + subsheet controls, but :280-282 filters `configureSheet` out whenever `getMEJType(this.document)` is truthy | Configure Sheet restored for plain JournalEntry documents only; deliberately hidden for MEJ-typed docs. Subsheet controls get `_handleContextClick` wiring (:284-296). No maximize control. |
| 4ef1b6c | Final review fixes: unnamed search input, scoped header controls, caught placement cancel | — | PRESENT-ALT (partial) | Header scope: :278-288 equivalent (window.controls contains only configureSheet). Placement: sheets/EncounterSheet.js:442 `await (EncounterTemplate.fromEncounter(this))?.drawPreview();` — no `.catch` | Encounter placement cancel still rejects unhandled. Search-input `name` removal is moot (no sidebar search input in 14.01, see defb902). |
| 964bf19 | Final review fix: swallow Enter in the sidebar search box | — | OBSOLETE | templates/sheets/partials/journal_sidebar.hbs has no `<search>`; sheets/JournalEntrySheet.js:375-383 SearchFilter binding still commented out | No sidebar search box exists to guard. |
| defb902 | Fix #661: restore page search in the journal sidebar | #661 | ABSENT | sheets/JournalEntrySheet.js:375-383 SearchFilter commented out; :831-856 `_onSearchFilter` commented out; templates/sheets/partials/journal_sidebar.hbs:1-4 no search input | Sidebar TOC page filter still missing. |
| 1d954d9 | Fix #661 follow-up: keep the nav-bar content search alongside the new sidebar filter | #661 | PRESENT-SAME | sheets/JournalEntrySheet.js:45-47 nav-bar `search` input control with `callback: this.searchText` retained | 14.01 never removed the nav-bar search; only the sidebar filter (defb902) is missing. |
| 35df79f | Fix: review polish | — | OBSOLETE | css/: no `.relationship-defunct` rule anywhere; sheets/EnhancedJournalSheet.js:2643-2647 relationship sync toggles `otherRelationship.hidden = !otherRelationship.hidden` without `items[id]` lookup | Polishes our own relationship-defunct feature, which 14.01 does not have. |

## Group 5 — quests, encounters, loot piles, slideshow, HUDs

| sha | title | issues | status | 14.01 evidence (file:line) | note |
|---|---|---|---|---|---|
| d9012b5 | Fix #803: skip reward-currency sync when the quest has no rewards | #803 | PRESENT-SAME | sheets/QuestSheet.js:441-448 | 14.01 wraps the rewards write in `if (Object.keys(rewards).length != 0)` and guards `if (reward)` before setting `reward.currency`; functionally equivalent (slightly stricter). |
| 8fa615a | Fix: crashes dropping actor folders on encounters, opening legacy person/place pages, and distributing loot to Item Piles | - | PRESENT-ALT (partial, 1 of 3) | present: sheets/EncounterSheet.js:357-361 (`dragData.type == 'Folder'` / `fromUuid(dragData.uuid)`); absent: sheets/EnhancedJournalSheet.js:2981 (`ptAvg.x / ptAvg.count` - `ptAvg` is never declared anywhere in the file); sheets/PersonSheet.js:89 and sheets/PlaceSheet.js:119 (`sheetSettings[attr].shown = ...` on `sheetSettings = {}` -> TypeError) | Encounter folder-drop part is fixed identically; Item Piles `ptAvg` ReferenceError and the legacy fields->attributes crash remain. |
| bf1578a | Fix: legacy person/place migration unreachable, and folder-of-actors drop force-constructed as Item | - | ABSENT | sheets/EnhancedJournalSheet.js:1915 `document = new CONFIG.Item.documentClass(data.data, {})`; sheets/PersonSheet.js:84 and sheets/PlaceSheet.js:114 `this.document.constructor.sheetSettings()` | `sheetSettings()` is defined on the sheet class (EnhancedJournalSheet.js:146 static / :152 instance), not on JournalEntryPage, so the migration branch still throws; folder-of-actors import (EncounterSheet.js:366 -> getItemData:2125 -> getDocument) still constructs Actors as Items. |
| 93a0813 | Fix: restore EncounterSheet methods swallowed by an unclosed comment in the v14 dead-code cleanup | - | N/A | sheets/EncounterSheet.js:69 `/*` ... :86 `}*/` (identical to base c007431:69/86) | Self-inflicted: the unclosed comment was introduced by our own v14 dead-code cleanup commit; neither base nor 14.01 ever had it. |
| 60cbc44 | Fix #790: revive synthetic drop forwarding, bind appv1 drop event, guard loot drops, and stop pf2e sheet-drop dropping purchase quantity | #790 | ABSENT | monks-enhanced-journal.js:1647-1653 (`journalSheetDrop` wrapper calls `getDragEventData(event)` but never defines `event` - only `...args`); monks-enhanced-journal.js:207 `["cyphersystem", "dnd4e", "dcc"]` (no pf2e); sheets/LootSheet.js:299 `_onDropLootItem(event, target)` re-parses the event and ignores forwarded data, :362 `item.actor.id` unguarded; sheets/ShopSheet.js:321 `_onDropItem(event)` ignores forwarded data | All four sub-fixes missing. Forwarded fake-event calls (e.g. hook at monks-enhanced-journal.js:4571) still hit `getDragEventData` on an event with no `dataTransfer`. |
| 97e368b | Fix: dropJournalSheetData forward crashed on loot-page targets | - | ABSENT | monks-enhanced-journal.js:4571 `sheet._onDropItem({...}, data)` with no `_onDropLootItem` fallback; sheets/LootSheet.js has no `_onDropItem` method (only `_onDropLootItem` at :299) | Loot-page target still throws "sheet._onDropItem is not a function". |
| 6b26c05 | Fix: quest objectives could not be drag-reordered (no drop target registered) | - | ABSENT | sheets/QuestSheet.js:337-345 (`_dragDrop` registers only `dropSelector: ".reward-items.items-list"`; the `.objective-items` selector at :92 is inside the commented-out appv1 defaultOptions); :615-632 reorder still uses `objectives.findIndex(...)` / `splice` on what is stored as an object keyed by id (array->object migration at :132) | No drop target for objectives, and the handler is array-shaped for object-shaped data. |
| f197dd5 | Fix: persist quest objective reorder past Foundry's flag merge | - | ABSENT | sheets/QuestSheet.js:631-632 plain `setFlag('monks-enhanced-journal', 'objectives', objectives)` | Same-key/same-value reorder still diffs to no-op. (Superseded in our tree by 45b0704's single ForcedReplacement write.) |
| 3f38e08 | Fix: restore objectives if the reorder rewrite fails mid-flight | - | ABSENT | sheets/QuestSheet.js:615-632 (no try/catch, no restore) | Intermediate fix; superseded in our tree by 45b0704 (no unset/set two-step any more), so only relevant as "14.01 has no reorder error handling". |
| 3eae5fd | Fix: final-review polish (loot drop arg, reorder failure feedback, getDocument comment) | - | ABSENT | sheets/LootSheet.js:452 `this._onDropLootItem(event, target)` (passes the HTMLElement); sheets/QuestSheet.js:615-632 no `ui.notifications.error`; sheets/EnhancedJournalSheet.js:1915 no comment | Polish on top of 60cbc44/bf1578a/3f38e08, all of which are absent. |
| c14631f | Fix: slideshow play controls bound document-wide and losing state after re-render; guard slide thumbnail cache | - | ABSENT | sheets/SlideshowSheet.js:317-319 `$('.nav-button.play').click(...)` etc. (no `html` scope); `activateListeners` (:290-323) never calls `updateButtons()` (callers only at :530/:563/:596); apps/slideconfig.js:178 and :293 write `this.options.journalentry._thumbnails[...]` unguarded (`_thumbnails` only created lazily at SlideshowSheet.js:252) | 14.01 has the Replay button and `_toggleDisabled` override (:279-282) but not these fixes. |
| 4cc119f | Fix: Quests HUD close button hidden by module CSS and scene-control toggle never refreshing | - | ABSENT (possibly intentional) | css/monks-journal-sheet.css:2613 `#objective-display header > :not(h1) { display: none !important; }` (hides the close control too); apps/objective-display.js:157-161 `close()` does nothing unless `options.properClose` (no `show-dialog` setting write, no `ui.controls.render` at all) | 14.01 also lacks our earlier close-branch that this commit polishes; the HUD is closable only via the scene-control toggle. UNSURE whether upstream considers the hidden close button a feature. |
| 45b0704 | Fix: objective reorder now a single atomic write (no flicker); Completed checkbox never saved | - | ABSENT | sheets/QuestSheet.js:631-632 plain `setFlag` (no `ForcedReplacement` anywhere in 14.01); templates/objectives.html:17 `<input type="checkbox" name="complete" {{checked document.status}} />` | Reorder no-ops (see f197dd5) and the Completed checkbox still posts as `complete` while the model reads `status`. |
| 381980f | Fix: player currency transfer crash duplicating loot money; Edit Currency corrupting the session cache and aliasing blank rows | - | ABSENT | apps/transfer-currency.js:141 `this.document.isOwner` (class only ever uses `this.options.document` at :111/:133/:142 - `this.document` is undefined -> TypeError for non-GM); apps/editcurrency.js:7 `this.currency = MonksEnhancedJournal.currencies` (aliases the live cache), :92 and :119 lookup by `dataset.id` (blank/duplicate ids alias rows), :125 aliases `defaultCurrencies`; templates/edit-currency.html:16 no `data-idx` | All three sub-fixes missing. |
| 8a07e8a | Fix: encounter placement cancel crashed and hung the placement promise; Note HUD offset half an icon from the pin | - | ABSENT | classes/encounter-template.js:173 `const { origin } = event.interactionData;` (raw PIXI stage events have no `interactionData`), :200 `this.layer._onDragLeftCancel(event)` in `_finishPlacement`, no `#origin` field; path is still live via sheets/EncounterSheet.js:442 `EncounterTemplate.fromEncounter(this)?.drawPreview()` -> encounter-template.js:115 `activatePreviewListeners`; classes/notehud.js:105-108 `_updatePosition` still subtracts `iconSize / 2` (NoteHUD instantiated at monks-enhanced-journal.js:4808) | 14.01's own placement code (`new cls(template, {parent: canvas.scene})` at EncounterSheet.js:493) is downstream of the same EncounterTemplate preview, so the cancel crash / hung promise remain. Blank control icon not counted per hint. |
| ca4c9cf | Revert incidental LevelDB pack churn from v14 testing | - | N/A | - | Repo hygiene (pack LevelDB files). |
| 915d037 | Update Polish translation | - | N/A | lang/pl.json (spot-check: 5 sampled added keys exist in 14.01) | Translation; values not compared. |
| 1d3832c | Chore: untrack browser-test artifacts and ignore local tool dirs | - | N/A | - | .gitignore / .playwright-mcp artifacts. |

## Discord `testing-enhanced-journal-v14` threads vs 14.01 and our fork (2026-09-18)

Source: 20 thread exports in `~/mej-v14-testing/` (16 re-exported 2026-09-18 20:14, 4 unchanged from Aug 15); transcripts in the job tmp dir. Static classification; "verify" = needs a live repro on 14.01. Ours = `integration-14.07` @ 4210173.

| # | Thread (reporter, date) | Maintainer reply | 14.01 | Our fork | Action |
|---|---|---|---|---|---|
| 1 | Players can't open the journal (n0p3_0ne, Aug 11) | — | FIXED (`allow-player` gate; c2) | fixed (14.04b) | closed |
| 2 | MATT Start Encounter (crow_guard, Aug 12) | — | FIXED for placement (c3 `new cls(template,{parent})`); cancel still crashes/hangs promise (`classes/encounter-template.js:173,200`) | fixed incl. cancel (8a07e8a, fix/encounter-placement-scene) | small PR: cancel path |
| 3 | removeAttribute Error (crow_guard, Aug 12; deterministic repro Sep 6: sidebar right-click → Edit → core UI) | — | SIDESTEPPED: c3 `SIDEBAR.Edit` override (`monks-enhanced-journal.js:5171-5189`) routes MEJ-typed entries away from the core sheet; crash itself (core `JournalEntrySheet._renderPageView`, MEJ page sheet yields no `element`) untouched; other routes to the core sheet remain — verify | NOT fixed: no Edit override, repro still open | adopt override on ours; root-cause why MEJ page sheets render no element under core's sheet |
| 4 | List, Create Entry (crow_guard, Aug 12; "Edit Item" dialog Save button overlap, Create Folder window) | — | unchanged, needs-info (no repro found Aug 15) | same | needs-info |
| 5 | Create Encounter Buttons (crow_guard, Aug 12) | icon intentionally blank (Sep 3) | FIXED (deprecated `iconSrc` gone) | fix/tableresult ok; `fix/encounter-controlicon` 57c14fc must be reverted per ruling | revert on ours |
| 6 | Relationship Header & Large Font Size (crow_guard, Aug 12, still Sep 5; also Customise Page headers) | "ignoring this one for right now" (Sep 6) | ABSENT | fixed (`fix/large-font-headers` 0cd3219, `height:auto`+`min-height`) | small PR |
| 7 | rollTable Deprecation (crow_guard, Aug 12) | — | FIXED (`switch (tableresult.type)`, EJS:2395) | fixed (fix/tableresult-deprecation) | closed |
| 8 | LICENSE missing (vandroid0555, Sep 3) | — | FIXED (LICENSE in tag) | n/a | closed |
| 9 | Sidebar click opens the previous tab (vandroid0555, Sep 3) | — | LIKELY STILL BROKEN: 1 s `_activatingTab` debounce drops overlapping activations; `removeDuplicateTabs()` runs before `tabs.active` exists (`apps/enhanced-journal.js:137`) — verify | fixed (b150f54 queue) | PR (medium: 9-hunk queue) |
| 10 | Dark Theme (crow_guard, Sep 5; links to an un-exported thread) | "still have the Dark Theme to get to" (Sep 12) | PARTIAL: only `.content.themed.theme-*` background var; zero `theme-dark` palette rules in css/ | `enh/dark-mode` 117-var palette (14.05-test) | PR (enhancement-sized) once linked thread is read |
| 11 | Actor Tabs (crow_guard, Sep 5; blank tab + warning + `fromDropData` TypeError from `JournalEntrySheet._onDrop`) | — | PARTIAL: shell drop now opens the actor sheet (c3, `apps/enhanced-journal.js:1487-1491`); subsheet `_onDrop` (`sheets/JournalEntrySheet.js:1018`) still has no type guard (core's doesn't either) — verify whether the drop still reaches it | NOT fixed: shell still opens a blank tab (`apps/enhanced-journal.js:1557-1562`); #771 fix covered links only | fix on ours; guard `_onDrop` in both |
| 12 | Right-Click Spam (crow_guard, Sep 5; `ContextMenu.close → _animate` on undefined element) | "I think this is a Foundry issue… not worth delaying" (Sep 12) | OPEN | same code | root-cause: both trees call `ui.journal._createContextMenu()` on every sidebar render (`activateDirectoryListeners`), core keeps no registry of prior instances |
| 13 | Popout (crow_guard, Sep 5/6; no hamburger/Detach on shell, no Detach on standalone sheets) | — | FIXED in 14.01 (**live-verified**: `_getHeaderControls()` returns `detach`/`attach` on both the shell and `PersonSheet`; c3 restored `super._getHeaderControls()`). My earlier static claim that the `window.controls` override drops core defaults was wrong. | NOT fixed: our `_getHeaderControls` never merges core controls | adopt on ours |
| 14 | Jump to Pin (crow_guard, Sep 5; Levels) | "Foundry bug" (Sep 12) | n/a | n/a | out of scope |
| 15 | Right-Click Edit? (crow_guard, Sep 5; question) | — | ANSWERED by c3 Edit override (MEJ when shell open, standalone sheet otherwise, Ctrl bypass) | no override | adopt on ours (see #3) |
| 16 | Clear Image (crow_guard, Sep 6; first click no-op until reopen, second click opens FilePicker + error) | — | UNCLEAR: c3 only moved the logic into `_onClearImage` (EJS:1431) and added the `text` type; neither symptom obviously addressed — verify | same original code | live repro on both |
| 17 | Progress List (crow_guard, Sep 6; fast +/- opens edit) | — | FIXED (`ignoreDblClick`, ListSheet.js:30) | absent | adopt |
| 18 | Search Bar Color (crow_guard, Sep 6; unreadable on none/clear bg, sort buttons) | "Foundry just randomly changing the names" (Sep 11) | FIXED (`.directory-header search input` selectors + `--mej-sidebar-button-color`) | verify our selectors | adopt/verify |
| 19 | Palette Button (crow_guard, Sep 6; `renderNoteConfig` fired for NotePalette) | fixed (Sep 7) | FIXED (`instanceof NotePalette` guard, mej.js:4640) | same double-hook bug expected | adopt |
| 20 | Encounter, Create Encounter Buttons 2 (crow_guard, Sep 11) | fixed in latest; warnings deferred to v15 | FIXED (release) | n/a | closed |

Scoreboard (after live verification): 14.01 resolves 11 of 20 (1, 2, 5, 7, 8, 13, 15, 17, 18, 19, 20); 8 remain open or partial in 14.01 (2-cancel, 3, 4, 6, 9, 10, 11, 12, 16); 1 out of scope (14). Our fork is behind 14.01 on 3/15, 11, 13, 17, 18, 19 and ahead on 2, 6, 9, 10.

## Live verification on stock 14.01 (2026-09-18, World A, Foundry 14.368, dnd5e 6.0.3, GM client, Playwright; no active canvas)

| Item | Result | Evidence |
|---|---|---|
| `currencyId(k)` with the string key `onAcceptOffer` passes | **CONFIRMED** | `helpers.currencyId('gp')` → `undefined`; `currencyId({id:'gp'})` → `'gp'` |
| `callCloseHooks(pageId)` hard-coded `"test"` lookup | **CONFIRMED** | TT 2-page core journal in the shell: with a page id, `getPageSheet` called 0×; with no id, called for both pages |
| `removeDuplicateTabs()` before `tabs.active` exists | **CONFIRMED** | saved tabs = two entries for the same entity, the duplicate active → `openJournalEntry` rejects `TypeError: this.tabs.active is not a function`, shell never opens |
| ImagePopout `_render(true)` on the LIMITED-permission open path | **CONFIRMED (API)** | `typeof ImagePopout.prototype._render === 'undefined'` (AppV2); the call at `monks-enhanced-journal.js:2431` throws for limited users |
| Detach/Attach header controls (Discord #13) | **NOT a defect in 14.01** | shell `_getHeaderControls()` = `[detach, attach]`; PersonSheet = `[detach, attach, configureOwnership, showPlayers, …]` |
| Clear Image (Discord #16) | **PARTIAL** | first Clear works (doc `src` → null, DOM shows `assets/person.png`); a second right-click on the image opens **no** context menu (binding lost after the re-render) — matches the "second try" complaint; the reporter's FilePicker/error half not reproduced |
| Large-font headers (Discord #6) | **REPRODUCED** | `uiConfig.fontScale = 10`: the Person detailed header clips the Role row (screenshot `v9-largefont.png`) |
| Right-click spam (Discord #12) | not reproduced | 24 + 40 rapid real right-clicks on shell sidebar items, 0 console errors |
| Actor drop onto the shell (Discord #11) | inconclusive | synthetic `DragEvent` and Playwright mouse drags do not carry HTML5 drag data into Foundry's DragDrop; needs a manual check |
| Sidebar click opens previous tab (Discord #9) | not testable here | requires an active canvas (reporter and our Sep 2 run: 3/3 with canvas on, 0/3 without); World A had no canvas in this session |
| Incidental | observed | one `EnhancedJournal._updatePosition … null.offsetWidth` page error while the shell was closed and re-opened rapidly (14.01 shell `setPosition` on a torn-down element) |
